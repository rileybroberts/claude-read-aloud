The prototype state is ready, and now it's your turn to test.

Open a second terminal and start Claude Code with the prototype plugin. Run the fast variant first, since that's the one the design is leaning toward. The command sets max thinking tokens to zero and points the plugin directory flag at the prototype read aloud say plugin in your worktrees. Then, if you have the patience, do one more run without the environment variable for item one only, so you can feel the thinking delay.

Ask something that produces a long response with a code block, a table, and a list, and work through this checklist. Jot a line per item and paste them back here. I'll pull the measured timings from the log myself.

First, first reading. Type slash read aloud when the response lands. Note the rough seconds to speech. Does the script sound like a person talking? How did it handle the code, table, and list? Any dead air between paragraphs? Second, screen footprint. What shows while the fork runs and when it finishes: a slash tasks footer hint, a task row, a relayed reading started line? Is that acceptable as the one line notice?

Third, stop. Mid reading, type slash read aloud stop. How does the blocked expansion render, plain line or error banner? Did speech stop within a second? Did anything land in the conversation? Fourth, replace. Mid reading, type slash read aloud again. Clean handover to the new reading?

Fifth, control verbs. Try slash read aloud status, slash read aloud auto on, slash read aloud auto off. Rendering only. Sixth, variants. Run slash read aloud brief on a long response, then slash read aloud with the show flag to see a script on screen.

Seventh, fast typing. Type slash read aloud the instant a response lands. Did it read the new one or the previous one? Eighth, quit mid reading. Exit Claude Code while speech plays. Expected behavior is that speech continues.

And last, gut verdict. Would you leave this on? What would make you turn it off? Do not run reset afterwards. I need the log intact to report the timings.
