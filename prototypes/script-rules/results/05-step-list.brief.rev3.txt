The prototype is ready to test: silent mode is off, no stale processes are running, Claude Code is version 2.1.269, and thinking tokens are not set, so the two variants will show different timing.

Open a second terminal and start Claude Code with the prototype plugin using the fast variant first, which is what the design favors. Run it with MAX_THINKING_TOKENS set to zero. Then, if you want to, run it once more without that environment variable for just the first item to feel the thinking delay.

Ask for something that produces a long response with a code block, a table, and a list. Work through the nine-item checklist: type the read-aloud command when the response lands and note roughly how many seconds until speech starts, whether the script sounds natural, and how it handled the code, table, and list. Check what shows on screen while the fork runs. Try stopping mid-reading, replacing with a new reading, and testing the control verbs like status, auto on, and auto off. Test the brief variant and the show flag. Type read-aloud the instant a response lands to see if it reads the new one or the previous one. Exit Claude Code while speech plays and expect speech to continue. Finally, give your gut verdict on whether you'd leave this on and what would make you turn it off. Jot one line per item and paste them back; the timings will be pulled from the log separately. Do not run reset afterwards.
