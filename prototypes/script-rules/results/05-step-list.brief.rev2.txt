The prototype is ready to test.

Silent mode is off, no stale processes are running, Claude Code is version 2.1.269, and the max thinking tokens variable is not set anywhere in your environment, so the two variants will differ the way the probes measured. Your part now. Open a second terminal and start Claude Code with the prototype plugin. Run the fast variant first, since that is the one the design is leaning toward. The command sets max thinking tokens to zero and points to the plugin directory in the prototype worktree.

Then, if you have the patience, run one more time without the environment variable for item 1 only, to feel the thinking delay. Ask something that produces a long response with a code block, a table, and a list, and work through the checklist. Jot a line per item and paste them back here. I will pull the measured timings from the log myself.

First, type the read aloud command when the response lands. Note rough seconds to speech, whether the script sounds like a person talking, how it handled the code, table, and list, and any dead air between paragraphs. Second, check what shows while the fork runs and when it finishes, whether a tasks footer hint, a task row, or a relayed reading started line appears, and whether that is acceptable as the one line notice. Third, mid-reading, send the stop command and check how the blocked expansion renders, whether speech stopped within a second, and whether anything landed in the conversation.

Fourth, mid-reading, send the read aloud command again and check for a clean handover to the new reading. Fifth, try the control verbs, status, auto on, and auto off, and check rendering only. Sixth, try the brief command on a long response, then the show flag to see a script on screen. Seventh, type the read aloud command the instant a response lands and check whether it read the new one or the previous one. Eighth, exit Claude Code while speech plays. The expected behavior is that speech continues. And ninth, your gut verdict: would you leave this on, and what would make you turn it off. Do not run reset afterwards. I need the log intact to report the timings.
