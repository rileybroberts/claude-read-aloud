The migration landed at three forty-seven in the morning UTC on April ninth twenty twenty-six, roughly eighteen months after the original RFC, and it touched about twelve hundred forty files across seven packages. Throughput went from about four hundred fifty requests per second to two thousand one hundred requests per second, p ninety-nine latency dropped sixty-two percent, from eight hundred ninety milliseconds to three hundred thirty-eight milliseconds, and our AWS bill fell by fourteen thousand five hundred dollars a month, which honestly nobody expected.

The tricky part wasn't the SQL. It was that PostgreSQL version sixteen point two changed how nulls distinct behaves in unique indexes, so about zero point three percent of rows that had quietly coexisted for years suddenly collided. We caught it in staging, thankfully, rolled back within eleven minutes, added a dedupe pass, and re-ran.

If you're doing something similar, read the release notes twice, keep the old read replica warm for at least forty-eight hours, and don't schedule a cutover on a Friday. For example, ours ran Thursday, that is we still had a full workday to fix things, versus the alternative, which is debugging a five hundred error storm at two in the morning on a Saturday while your on-call engineer's phone is on do not disturb.
