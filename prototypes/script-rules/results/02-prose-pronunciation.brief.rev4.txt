The migration landed on April 9th, 2026 at 3:47 a.m.

UTC and cut p ninety-nine latency by 62 percent while trimming the AWS bill by 14,500 dollars a month. It touched about 1,240 files across 7 packages and pushed throughput from roughly 450 requests per second up to 2,100 requests per second.

The tricky part wasn't SQL; it was that PostgreSQL version 16.2 changed how the NULLS DISTINCT behavior works in unique indexes, so about 0.3 percent of rows that had coexisted quietly for years suddenly collided.

They caught it in staging and rolled back within 11 minutes, then added a dedupe pass and re-ran. If you're doing something similar, read the release notes twice, keep the old read-replica warm for at least 48 hours, and don't schedule a cutover on a Friday.

The migration ran Thursday, which meant they had a full workday to fix things instead of debugging errors at 2 a.m. on a Saturday.
